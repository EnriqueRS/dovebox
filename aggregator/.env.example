# DoveBox Aggregator + MCP Bridge env vars
# Copy this to .env (not committed) and fill in your values:
#   cp .env.example .env
# Then: docker compose up -d

# Home Assistant long-lived access token (obligatorio)
HASS_TOKEN=